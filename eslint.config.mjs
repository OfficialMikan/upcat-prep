import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // These experimental React Compiler rules flag the standard documented
      // "fetch data in useEffect on mount" pattern as an error. We use this
      // pattern intentionally for simple client-side data loading (no React
      // Compiler adoption yet), so we downgrade to warnings rather than
      // rewrite straightforward data-fetching effects into more complex forms.
      "react-hooks/set-state-in-effect": "warn",
      "react-hooks/immutability": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
